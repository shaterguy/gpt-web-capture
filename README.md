# GPT Web Capture

Standalone Android WebView diagnostics recorder for ChatGPT web UI investigations.

Development work is performed on versioned DEV branches. The default branch is only the repository bootstrap baseline until a verified candidate is promoted.
